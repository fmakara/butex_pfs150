#!/bin/bash

#sudo apt install make sdcc # NOPE: it needs sdcc v4.2 at least
# just because it bugs with a "dec" operation
# so download from https://sdcc.sourceforge.net/snap.php#Linux
# decompress, and add the bin folder to PATH

echo "Done! Now build it with 'make'"